A type system and it's compiler. This type system support both C++ and Python API, sharing the same memory underneath, avoiding conversion between C++ and Python. It has it's own serialization/deserialization format. By use block-serialization mechanism, the serialization/derserialization is much faster.